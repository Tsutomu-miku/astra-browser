{
  "name": "astra-browser",
  "version": "0.1.12",
  "private": true,
  "description": "A Chromium-based browser inspired by Zen and Arc.",
  "author": {
    "name": "Tsutomu-miku",
    "email": "Tsutomu-miku@users.noreply.github.com"
  },
  "main": "src/main/main.js",
  "scripts": {
    "dev": "concurrently -k \"vite\" \"wait-on http://127.0.0.1:5173 && VITE_DEV_SERVER_URL=http://127.0.0.1:5173 electron .\"",
    "start": "pnpm build && electron .",
    "build": "vite build",
    "clean:release": "node scripts/clean-release.mjs",
    "clean:package-output": "node scripts/clean-package-output.mjs",
    "package": "pnpm clean:release && pnpm build && electron-builder --publish never && pnpm clean:package-output",
    "package:all": "pnpm clean:release && pnpm build && electron-builder --mac dmg zip --x64 --publish never && electron-builder --mac dmg zip --arm64 --publish never && electron-builder --win portable --x64 --publish never && electron-builder --win portable --arm64 --publish never && electron-builder --linux AppImage deb tar.gz --x64 --publish never && pnpm clean:package-output",
    "package:mac": "pnpm clean:release && pnpm build && electron-builder --mac dmg zip --publish never && pnpm clean:package-output",
    "package:mac:arm64": "pnpm clean:release && pnpm build && electron-builder --mac dmg zip --arm64 --publish never && pnpm clean:package-output",
    "package:mac:x64": "pnpm clean:release && pnpm build && electron-builder --mac dmg zip --x64 --publish never && pnpm clean:package-output",
    "package:mac:all": "pnpm clean:release && pnpm build && electron-builder --mac dmg zip --x64 --publish never && electron-builder --mac dmg zip --arm64 --publish never && pnpm clean:package-output",
    "package:linux": "pnpm clean:release && pnpm build && electron-builder --linux AppImage deb tar.gz --x64 --publish never && pnpm clean:package-output",
    "package:win": "pnpm clean:release && pnpm build && electron-builder --win portable --publish never && pnpm clean:package-output",
    "package:win:arm64": "pnpm clean:release && pnpm build && electron-builder --win portable --arm64 --publish never && pnpm clean:package-output",
    "package:win:x64": "pnpm clean:release && pnpm build && electron-builder --win portable --x64 --publish never && pnpm clean:package-output",
    "package:win:all": "pnpm clean:release && pnpm build && electron-builder --win portable --x64 --publish never && electron-builder --win portable --arm64 --publish never && pnpm clean:package-output",
    "typecheck": "tsc --noEmit",
    "lint": "node scripts/check-sources.mjs",
    "test": "vitest run",
    "check": "pnpm lint && pnpm typecheck && pnpm test && pnpm build"
  },
  "dependencies": {
    "react": "^19.2.6",
    "react-dom": "^19.2.6",
    "react-icons": "^5.6.0",
    "zustand": "^5.0.13"
  },
  "devDependencies": {
    "@types/react": "^19.2.15",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.2",
    "concurrently": "^9.2.1",
    "electron": "^42.2.0",
    "electron-builder": "^26.8.1",
    "jsdom": "^29.1.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.14",
    "vitest": "^4.1.7",
    "wait-on": "^9.0.10"
  },
  "build": {
    "appId": "app.astra.browser",
    "productName": "Astra Browser",
    "asar": true,
    "compression": "maximum",
    "npmRebuild": false,
    "electronLanguages": [
      "en-US",
      "zh-CN",
      "zh-TW"
    ],
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/renderer/**/*",
      "src/main/**/*",
      "package.json"
    ],
    "extraMetadata": {
      "main": "src/main/main.js"
    },
    "mac": {
      "category": "public.app-category.productivity",
      "identity": null,
      "target": [
        "dmg",
        "zip"
      ],
      "artifactName": "Astra-Browser-${version}-mac-${arch}.${ext}"
    },
    "win": {
      "target": [
        {
          "target": "portable",
          "arch": [
            "x64",
            "arm64"
          ]
        }
      ],
      "artifactName": "Astra-Browser-${version}-win-${arch}.${ext}"
    },
    "linux": {
      "category": "Network",
      "maintainer": "Tsutomu-miku <Tsutomu-miku@users.noreply.github.com>",
      "target": [
        {
          "target": "AppImage",
          "arch": [
            "x64"
          ]
        },
        {
          "target": "deb",
          "arch": [
            "x64"
          ]
        },
        {
          "target": "tar.gz",
          "arch": [
            "x64"
          ]
        }
      ],
      "artifactName": "Astra-Browser-${version}-linux-${arch}.${ext}"
    }
  }
}
